import EStyleSheet from 'react-native-extended-stylesheet';

const styles = EStyleSheet.create({
  itemContainerStyle: {
    height: 56,
    borderBottomWidth: 1,
    borderBottomColor: '$grayRegular',
  },
});

export default styles;
